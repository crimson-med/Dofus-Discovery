extends layout

block content
  h1= title
  p Welcome to #{title}
  table
    thead
      tr
        th Key
        th X
        th Y
        th Map
    tbody
      each val in json
        tr
          td #{val}
