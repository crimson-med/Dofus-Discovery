extends layout

block content
  h1= title
  p Welcome to #{title}
  table
    thead
      tr
        th Key
        th X
        th Y
        th Map
    tbody
      each index in json
        tr
          td #{index}
